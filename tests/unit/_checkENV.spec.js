describe('log', () => {
	it('renders li for each item in props.items', () => {
console.log('-------------------------------')
		console.log(process.env.VUE_CLI_BABEL_TARGET_NODE)
		console.log(process.env.VUE_CLI_BABEL_TRANSPILE_MODULES)
	})

})